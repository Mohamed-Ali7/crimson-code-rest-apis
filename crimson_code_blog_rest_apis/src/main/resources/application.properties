spring.datasource.url=${DATASOURCE_URL}
spring.datasource.username=${DATASOURCE_USER}
spring.datasource.password=${DATASOURCE_PASSWORD}

frontendVerifyEmailUrl=${FRONTEND_VERIFY_EMAIL_URL}
frontendPasswordResetUrl=${FRONTEND_PASSWORD_RESET_URL}

spring.servlet.multipart.max-file-size=2MB
spring.servlet.multipart.max-request-size=3MB

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.springframework.web: DEBUG

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=crimson.code.email@gmail.com
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=
spring.mail.properties.mail.smtp.starttls.enable=true

tokenSecert=${JWT_SECRET}

# 10 day
emailVerificationTokenExpirationDate=864000000

# 10 minutes
accessTokenExpirationDate=600000

# 10 days
refreshTokenExpirationDate=864000000

# 1 day
passwordResetTokenExpirationDate=86400000
